window.addEventListener('load', () => {
  const typed = new Typed('#main_typed', {
    stringsElement: '#typed_string',
    typeSpeed: 110,
    backSpeed: 100,
    backDelay: 1000,
    startDelay: 800,
    loop: true,
    loopCount: Infinity,
  });
});
